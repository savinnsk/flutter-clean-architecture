name: flutter_application_1
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=2.18.5 <3.0.0'
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ## to run test without flutter
  mockito: ## to create mocks versions
  faker:  ## to create a fake values
  flutter_lints: ^2.0.0


flutter:
  uses-material-design: true